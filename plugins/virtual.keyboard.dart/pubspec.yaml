name: vk
description: Redesigned virtual keyboard for dart + flutter, forked from virtual keyboard
version: 0.2.1
homepage: https://github.com/Taha-Firoz/virtual.keyboard.dart

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
